function verifyInput(){
    var username = "my name";
    var password = "password";
    if(document.getElementById('uName').value==username)
    {
        document.getElementById("response").innerHTML = "Welcome, " + username;
    }
    //add code here if the username is bad

    //modify the code above to check the password as well as the username
    //and return appropriate messages
}